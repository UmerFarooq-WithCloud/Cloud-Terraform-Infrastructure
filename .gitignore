# Private keys - NEVER commit
private_key/
*.pem

# Terraform variable files with sensitive values
*.tfvars
*.tfvars.json

# Terraform state files - contain sensitive infrastructure data
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Terraform working directory
.terraform/
.terraform.lock.hcl

# Local exec output logs
record_provider_data/

# OS files
.DS_Store
Thumbs.db
